export enum NotificationTypeEnum {
    success='success',
    warning='warning',
    info='info',
    error='error'
}

export enum StatusEnum {
    enable=1,
    disable=0
}

export enum IsDeleteEnum {
    true=1,
    false=0
}